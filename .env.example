# Copy this file to .env and fill in your values

# Session
SESSION_SECRET=some_long_random_string

# SMTP Configuration (for email verification)
# Leave blank to log verification codes to the console instead
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
