SESSION_SECRET=some_long_random_string

# SMTP Configuration (for email verification)
# Leave these blank to log verification codes to the console instead of sending emails
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
